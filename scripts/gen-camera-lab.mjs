#!/usr/bin/env node
import{mkdirSync,writeFileSync}from"node:fs";import{join}from"node:path";const root=new URL("..",import.meta.url).pathname;const c={ja:["カメラ工房","世界の座標と画面の座標を分け、追従・揺れ・部屋移動をゲームルールから独立して作ります。"],en:["Camera Lab","Separate world and screen coordinates, then build follow, shake, and room movement independently from game rules."]};for(const lang of["ja","en"]){const q=c[lang],o=lang==="ja"?"en":"ja",d=join(root,"web",lang,"labs","camera");mkdirSync(d,{recursive:true});writeFileSync(join(d,"index.html"),`<!doctype html><html lang="${lang}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${q[0]} | Ebi Showcase</title><link rel="stylesheet" href="../../../style.css"></head><body><header class="nav"><a class="brand" href="../../"><span>EBI</span> SHOWCASE</a><nav><a class="lang" href="../../../${o}/labs/camera/">${o.toUpperCase()}</a></nav></header><main><section class="track-hero"><span class="track-letter">CA</span><div><p class="eyebrow">CAMERA / COMPOSITION</p><h1>${q[0]}</h1><p>${q[1]}</p></div></section><section class="lesson-section"><h2>world → camera → screen</h2><p>${lang==="ja"?"カメラはゲーム世界を描くための座標変換です。判定用の世界座標を変えずに、見せ方だけを変えられます。":"A camera transforms the world for drawing. It changes presentation without changing world coordinates used for rules."}</p></section></main></body></html>`)}console.log("Generated bilingual Camera Lab hub.");
