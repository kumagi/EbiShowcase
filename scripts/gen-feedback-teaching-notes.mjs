#!/usr/bin/env node
// Copyright 2026 Ebi Showcase contributors. Licensed under Apache-2.0.
// Add small, route-specific visual explanations requested by reader feedback.
import { existsSync, readFileSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const root = new URL("..", import.meta.url).pathname;
const esc = (s) => String(s).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;");
const code = (s, label) => `<pre class="feedback-note-code"${label ? ` aria-label="${label}"` : ""}>${esc(s.replaceAll("\\n", "\n"))}</pre>`;
const flow = (s, label) => `<div class="feedback-note-flow" role="img" aria-label="${label}">${s}</div>`;
const note = (eyebrow, title, text, visual) => ({ eyebrow, title, text, visual });
const notes = {
  "guides/setup": { ja: note("はじめての見取り図", "文字を入力する窓と、ゲームの窓は別もの", "PowerShellとターミナルは名前や色が違っても、文字で命令を出す同じ役割です。go.modはゲームの名前（モジュール名）と借りる部品を書いた目次。実行すると、今度はEbitengineのゲーム画面が開きます。", flow("<span>PowerShell / ターミナル</span><b>→ go run . →</b><strong>中身のないゲーム画面</strong>", "命令を入力する窓からゲーム画面へ")), en: note("A first map", "The command window and the game window are different", "PowerShell and Terminal may look different, but both accept typed commands. go.mod is the table of contents with your module name and borrowed parts. Running the program opens a separate Ebitengine game window.", flow("<span>PowerShell / Terminal</span><b>→ go run . →</b><strong>empty game window</strong>", "from command window to game window")) },
  "guides/testing/regression-tests": { ja: note("再現盤面を目で見る", "小さな盤面を、そのままテストの材料にする", "色の記号を並べた盤面なら、どの並びを再現したのか一目で分かります。同じ盤面データを関数へ渡すと、画面を起動しなくても同じ答えを毎回確認できます。", `${code("A B C A B\\nB C C C A\\nA B C B A\\nC C A B C\\nB A B C A", "3マッチの再現盤面")}<p class="feedback-note-caption">Cが3つ並ぶ → 消去の答えをテストする</p>`), en: note("See the reproduction board", "Make the small board the test input", "A board made from simple symbols shows exactly which arrangement caused the bug. Pass the same board data to the rule and check the same answer every time without opening the game.", `${code("A B C A B\\nB C C C A\\nA B C B A\\nC C A B C\\nB A B C A", "match-three reproduction board")}<p class="feedback-note-caption">Three C cells → test the clear result</p>`) },
  "games/tap-target": { ja: note("ゲームの事実 → 見せ方", "先にゲームを進め、表示はその事実を読む", "Updateは得点や位置など、ゲームの事実を進めます。Drawはそれを変えず、呼ばれた時に画面へ見せます。すぐ下の動く見本では、一つのゲーム状態を違う描き方で同時に表示しています。", code("UPDATE: score / position\\n          ↓\\n        game\\n          ↓\\nDRAW: 画面にどう見せるか", "ゲームの事実と表示の役割分担")), en: note("Game facts → presentation", "Advance the game first; presentation reads those facts", "Update advances game facts such as score and position. Draw does not change them; it presents them whenever called. The live example below shows one game state through different presentations at the same time.", code("UPDATE: score / position\\n          ↓\\n        game\\n          ↓\\nDRAW: choose how to present it", "game facts and presentation")) },
  "games/timing-meter": { ja: note("Updateの刻みと状態", "同じボタンでも、今の状態で意味が変わる", "Updateは設定された一定の刻みで状態を少しずつ進めます。状態機械は「動く／止める／結果」の札を持ち、ラウンドごとに左右を交代すると同じタイミングの暗記ゲームになりません。Drawの回数はこのルールを変えません。", flow("<span>動く</span><b>→ ボタン →</b><strong>止める</strong><b>→ 次のラウンド →</b><span>動く</span><code>round % 2</code>", "状態の移り変わり")), en: note("Update ticks and state", "The same button can mean different things", "Update advances state in small, configured ticks. A state machine keeps a label such as moving, stopped, or result. Alternating the starting side prevents a memorized timing trick, and Draw frequency does not change this rule.", flow("<span>moving</span><b>→ button →</b><strong>stopped</strong><b>→ next round →</b><span>moving</span><code>round % 2</code>", "state transitions")) },
  "games/catch-stars": { ja: note("見えない箱", "矩形判定は、星とカゴの箱が重なったかを見る", "見た目が丸い星でも、最初は四角い箱で十分です。カゴは左右に動かすゲームなのでXだけを変え、Yは落ち着いた高さに固定すると判定を考えやすくなります。", flow("<span>★ 星の箱</span><b>重なる？</b><strong>▰ カゴの箱 = HIT</strong><code>basket.xだけ変える</code>", "星とカゴの矩形が重なる図")), en: note("Invisible boxes", "A box hit checks whether the star and basket overlap", "Even a round-looking star can start as a simple rectangle. The basket only needs horizontal movement, so keep Y at a steady height and change X with the pointer or keys.", flow("<span>★ star box</span><b>overlap?</b><strong>▰ basket box = HIT</strong><code>change basket.x only</code>", "star and basket boxes overlap")) },
  "games/flappy": { ja: note("重力を感じる", "重力は、Updateのたびに下向きの速さを少し足す", "速度が整数だけだと動きが急に見えます。0.42のような小数を使うと、細かなUpdateの刻みが積み重なり、なめらかな落下の弧になります。順番は速度を変える→位置を変えるです。", flow("<span>速度 -7.4</span><b>+ 重力 0.42</b><strong>速度 -6.98</strong><b>→ 位置を更新</b><span>次のUpdate</span>", "重力が速度へ加わる順番")), en: note("Feel gravity", "Gravity adds a little downward speed on each Update", "Integer-only movement looks abrupt. A value such as 0.42 lets small Update ticks build into a smooth falling arc. First change speed, then change position.", flow("<span>speed -7.4</span><b>+ gravity 0.42</b><strong>speed -6.98</strong><b>→ move position</b><span>next Update</span>", "gravity added to speed")) },
  "games/pong": { ja: note("ひとりでも練習", "CPUは、ひとりで反射を試すための相手", "CPUのパドルがボールを追うので、もう一人いなくても反射とベクトルを試せます。vxはX方向、vyはY方向の速さです。壁に当たったら該当する符号を反転します。", flow("<code>vx = 横の速さ</code><code>vy = 縦の速さ</code><b>壁 → vy = -vy</b><strong>跳ね返る</strong>", "速度成分と反射")), en: note("Practice alone", "The CPU is your practice opponent", "The CPU paddle follows the ball, so you can practice reflections without another player. vx is horizontal speed and vy is vertical speed. Flip the matching sign when the ball hits a wall.", flow("<code>vx = horizontal</code><code>vy = vertical</code><b>wall → vy = -vy</b><strong>bounce</strong>", "velocity components and reflection")) },
  "games/space-shooter": { ja: note("種類ごとのリスト", "Enemyの形を先に決めると、i番目の意味が見える", "敵だけ動かす、弾だけ消す、という処理を分けるためにスライスを種類ごとに持ちます。<code>i</code>はそのリストの何番目かを表す番号です。", code("type Enemy struct {\\n    X, Y float64\\n    HP   int\\n}\\nfor i := range enemies {\\n    enemies[i].X -= 2\\n}", "Enemy構造体とi番目の敵")), en: note("Lists by kind", "Define Enemy so the meaning of i is clear", "Keep enemies and bullets in separate slices so each group can be updated or removed independently. <code>i</code> is the index: which item in that list we are reading.", code("type Enemy struct {\\n    X, Y float64\\n    HP   int\\n}\\nfor i := range enemies {\\n    enemies[i].X -= 2\\n}", "Enemy struct and index i")) },
  "games/breakout": { ja: note("i番目を全部描く", "配列の中身は、forで1個ずつ画面へ出す", "<code>i</code>は「何番目のブロックか」を表します。forループで0番目から最後まで順にDrawすれば、同じ描画ルールをたくさんのブロックへ使い回せます。", code("for i := range blocks {\\n    if blocks[i].Alive {\\n        drawBlock(blocks[i])\\n    }\\n}", "ブロックを順番に描くforループ")), en: note("Draw every index", "A for loop draws each item in the slice", "<code>i</code> means which block we are visiting. A loop walks from the first block to the last and reuses one drawing rule for all of them.", code("for i := range blocks {\\n    if blocks[i].Alive {\\n        drawBlock(blocks[i])\\n    }\\n}", "for loop drawing every block")) },
  "games/snake": { ja: note("方眼紙として考える", "480pxを32pxで割ると、15×15マス", "グリッドは画面を同じ大きさのマスへ分ける方法です。新しい頭を先頭へ追加し、エサを食べていなければ最後の尾を1個外す、という履歴の流れを図にします。", code("480 ÷ 32 = 15\\nnew head → [●][●][●] → drop tail\\n             先頭        最後", "15×15グリッドと蛇の履歴")), en: note("Think in squares", "480 px divided by 32 px makes a 15×15 grid", "A grid divides the screen into equal cells. Add the new head at the front of the history, then remove one tail cell when no food was eaten.", code("480 ÷ 32 = 15\\nnew head → [●][●][●] → drop tail\\n             front       last", "15 by 15 grid and snake history")) },
  "games/sokoban": { ja: note("記号の地図", "文字列は、ステージの設計図を読みやすくする", "1行を1行分のマスとして並べると、空白や数字の表より壁と箱の形を目で確認できます。起動時に各文字を読み、ゲームのタイルデータへ変換します。", code("# 壁   @ プレイヤー\\n$ 箱  . ゴール\\n########\\n#@ $.  #\\n########", "倉庫番の記号マップ")), en: note("A map made of symbols", "Strings make a stage blueprint easy to read", "Put one row of cells in each string. You can see walls and boxes at a glance, then convert each character into tile data when the game starts.", code("# wall  @ player\\n$ box   . goal\\n########\\n#@ $.  #\\n########", "Sokoban symbol map")) },
  "games/platformer": { ja: note("重なりを先に見る", "衝突はXを直してから、Yを直す", "まず2つの箱が重なったかを見ます。横のめり込みをXだけ戻し、次に縦のめり込みをYだけ戻すと、床の角でも滑らかに着地できます。gravityの小数は落下を滑らかにするためです。", flow("<span>重なった？</span><b>→ Xを分離</b><b>→ Yを分離</b><strong>着地</strong>", "XとYを別に解決する順番")), en: note("See overlap first", "Resolve X first, then resolve Y", "First check whether the two boxes overlap. Move out on X only, then on Y only, so a corner does not break landing. A decimal gravity value makes the fall smooth.", flow("<span>overlap?</span><b>→ separate X</b><b>→ separate Y</b><strong>land</strong>", "resolve X and Y separately")) },
  "games/dungeon-crawler": { ja: note("用語とデータ", "ダンジョンは、洞窟のような迷路ゲーム", "ダンジョンという言葉は、部屋や通路が続く探索場所のことです。マップをデータから読むと、同じ移動ルールのまま別の洞窟を増やせます。", flow("<span>map.json</span><b>→ 読み込む</b><strong>洞窟の部屋</strong><b>→ 同じルールで探索</b>", "マップデータから洞窟を作る流れ")), en: note("Words and data", "A dungeon is a cave-like maze to explore", "A dungeon is an exploration place made of rooms and paths. Loading a map from data lets you add another cave without rewriting movement rules.", flow("<span>map.json</span><b>→ load</b><strong>cave rooms</strong><b>→ explore with one rule</b>", "map data to cave rooms")) },
  "games/bullet-hell": { ja: note("まず1発、次に大量", "弾幕も、1発ずつの円をforで描く", "最初は弾1個の位置と速度をUpdateし、Drawで円を1つ描きます。動くことを確認してから、forで同じ処理を何十個にも増やし、最後に画面外を削除します。", code("for i := range bullets {\\n    bullets[i].X += bullets[i].VX\\n    drawCircle(bullets[i].X, bullets[i].Y)\\n}", "弾を一発ずつ更新して描く")), en: note("One shot, then many", "A bullet hell still draws one circle at a time", "Start with one bullet: update its position and draw one circle. Once that works, use a for loop for dozens, then remove bullets outside the screen.", code("for i := range bullets {\\n    bullets[i].X += bullets[i].VX\\n    drawCircle(bullets[i].X, bullets[i].Y)\\n}", "update and draw one bullet at a time")) },
  "tracks/visual-effects/vfx-stamp": { ja: note("UpdateとDrawを分ける", "画像は一度用意し、Drawで置き場所だけ変える", "画像を読む処理は初期化で一度だけ。tickごとにUpdateで座標を決め、DrawでDrawImageOptionsのGeoM.Translateへ渡します。", flow("<span>初期化：画像を読む</span><b>→ Update：座標</b><b>→ Draw：Translate</b><strong>画面へ描く</strong>", "画像を読む、座標を決める、描く")), en: note("Separate Update and Draw", "Load the image once; change only its place in Draw", "Read the image once during setup. Each tick, Update chooses coordinates and Draw passes them to GeoM.Translate in DrawImageOptions.", flow("<span>setup: load image</span><b>→ Update: coordinates</b><b>→ Draw: Translate</b><strong>paint</strong>", "load, position, and draw")) },
  "tracks/visual-effects/vfx-transform": { ja: note("pivotをたとえる", "pivotは、タイヤを回すネジ頭のようなもの", "左上を軸に回すと絵が遠くへ飛びます。絵の中心を原点へ寄せ、RotateとScaleを使い、最後に置きたい場所へ戻す順番を守ります。", flow("<span>中心を原点</span><b>→ Rotate / Scale</b><strong>→ 置く場所へTranslate</strong>", "pivotを中心に変換する順番")), en: note("A pivot example", "A pivot is like the screw at the center of a tire", "Rotating around the top-left makes the picture swing away. Move its center to the origin, rotate and scale, then translate it to the final place.", flow("<span>center to origin</span><b>→ Rotate / Scale</b><strong>→ Translate to place</strong>", "transform around the pivot")) },
  "tracks/visual-effects/vfx-tint": { ja: note("色を数字で見る", "色はRGBの3つの数字、alpha 0.5は半透明", "赤を(1,0,0)、緑を(0,1,0)のように考えます。乗算で0.5を掛けると、その色の明るさが半分になります。alphaが0.5なら背景が半分透けます。", code("赤 (1,0,0) × 0.5 = (0.5,0,0)\\nalpha 0.5 = 半分だけ見える", "RGBとalphaの計算例")), en: note("See color as numbers", "RGB uses three numbers; alpha 0.5 means half transparent", "Think of red as (1,0,0) and green as (0,1,0). Multiplying by 0.5 makes a channel half as bright. Alpha 0.5 lets half of the background show through.", code("red (1,0,0) × 0.5 = (0.5,0,0)\\nalpha 0.5 = half visible", "RGB and alpha example")) },
  "tracks/visual-effects/vfx-additive": { ja: note("加算を計算する", "赤と緑を足すと黄色、全部足すと白", "加算合成は光の色を足す方法です。赤(1,0,0)+緑(0,1,0)=(1,1,0)で黄色。赤・緑・青を足すと(1,1,1)で白になります。", code("赤 + 緑 = (1,0,0) + (0,1,0)\\n      = (1,1,0) = 黄色\\n赤 + 緑 + 青 = 白", "加算合成の計算例")), en: note("Calculate additive color", "Red plus green makes yellow; all three make white", "Additive blending adds light colors. Red (1,0,0) plus green (0,1,0) becomes (1,1,0), yellow. Add blue too and the result is white.", code("red + green = (1,0,0) + (0,1,0)\\n          = (1,1,0) = yellow\\nred + green + blue = white", "additive blending example")) },
  "tracks/visual-effects/vfx-walk": { ja: note("スプライトのコマ", "並んだ絵の番号をframeで選ぶ", "歩きの画像を横に並べたものがスプライトシートです。frame++は次に表示する画像の番号を進める処理で、余りを使えば最後から最初へ戻れます。", flow("<span>0</span><span>1</span><span>2</span><span>3</span><b>frame++ → 次の絵</b>", "歩きアニメーションの4コマ")), en: note("Sprite frames", "Use frame to choose one picture from a row", "A sprite sheet places walk pictures side by side. frame++ advances the picture number; a remainder sends it back to the first frame.", flow("<span>0</span><span>1</span><span>2</span><span>3</span><b>frame++ → next picture</b>", "four walk animation frames")) },
  "tracks/visual-effects/vfx-particles": { ja: note("粒に命を入れる", "煙・火花・ほこりを、少し違う方向へ飛ばす", "パーティクルは小さな粒のデータです。乱数で少しずつ違う角度と速さを与えると、全粒が同じ直線へ進まず、煙や火花らしく見えます。", flow("<span>✦</span><span>·</span><span>✧</span><span>•</span><b>random angle → 自然な広がり</b>", "乱数で方向が違う火花")), en: note("Give particles life", "Smoke, sparks, and dust need slightly different directions", "A particle is a tiny piece of data. Randomly vary its angle and speed so every dot does not travel in one straight line; the burst then looks like smoke or sparks.", flow("<span>✦</span><span>·</span><span>✧</span><span>•</span><b>random angle → natural spread</b>", "sparks with random directions")) },
};

function section(noteValue, route) {
  const id = `feedback-note-${route.replaceAll("/", "-")}`;
  return `<section class="feedback-teaching-note" aria-labelledby="${id}"><div><p class="eyebrow">${noteValue.eyebrow}</p><h2 id="${id}">${noteValue.title}</h2><p>${noteValue.text}</p></div><div class="feedback-note-visual">${noteValue.visual}</div></section>`;
}

function inject(html, noteValue, route) {
  const marker = `<!-- feedback-note:${route}:start -->`;
  const end = `<!-- feedback-note:${route}:end -->`;
  const escape = (value) => value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  let next = html.replace(new RegExp(`${escape(marker)}[\\s\\S]*?${escape(end)}\\n?`, "g"), "");
  const block = `${marker}\n${section(noteValue, route)}\n${end}`;
  if (route === "games/tap-target") {
    const anchor = "<!-- renderer-freedom:start -->";
    return next.includes(anchor) ? next.replace(anchor, `${block}\n${anchor}`) : next;
  }
  const beginner = next.indexOf('<section class="beginner-bridge">');
  const start = beginner >= 0 ? beginner : next.search(/<section class="(?:test-step-hero|setup-hero|overview-hero|data-hero)"/);
  if (start < 0) return next;
  const close = next.indexOf("</section>", start);
  return next.slice(0, close + "</section>".length) + `\n${block}` + next.slice(close + "</section>".length);
}

let updated = 0;
for (const [route, localized] of Object.entries(notes)) {
  for (const lang of ["ja", "en"]) {
    const file = join(root, "web", lang, route, "index.html");
    if (!existsSync(file)) continue;
    const html = readFileSync(file, "utf8");
    const next = inject(html, localized[lang], route);
    if (next !== html) { writeFileSync(file, next); updated++; }
  }
}
console.log(`Added ${updated} bilingual feedback teaching notes.`);
