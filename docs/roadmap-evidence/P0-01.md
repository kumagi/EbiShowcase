# P0-01 — Synchronize Go 1.25 across `go.mod`, README, AGENTS, and CI.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Files: `go.mod`, `README.md`, `AGENTS.md`, `.github/workflows/pages.yml`, `scripts/gen-setup-guide.mjs`, and regenerated `web/{ja,en}/guides/setup/index.html`.
- Behavior: the module declares Go 1.25.0; local documentation and the bilingual installation guide require Go 1.25+; GitHub Pages uses `go-version-file: go.mod`, so CI reads the same baseline.

## Commands and results

```text
rg -n "1\\.24|go1\\.24|Go 1\\.2[0-4]" --glob '!dist/**' --glob '!vendor/**' --glob '!docs/roadmap-evidence/**' .
exit 1 (no stale Go 1.24-or-earlier baseline references)

go version
go version go1.26.5 darwin/arm64 (local toolchain satisfies the Go 1.25 baseline)

node scripts/gen-setup-guide.mjs
wrote web/ja/guides/setup/index.html
wrote web/en/guides/setup/index.html

git diff --check
exit 0
```

## Manual checks

- Viewport / input: not applicable; this task changes the documented build baseline only.
- Japanese / English: both generated setup guides now say `Go 1.25` / `Go 1.25 or later` and show a Go 1.25 version example.
- Screenshots: not applicable; no rendered game or layout changed.
- CI: `.github/workflows/pages.yml` uses `actions/setup-go@v6` with `go-version-file: go.mod`, which resolves the declared `go 1.25.0` baseline without duplicating a version string.
