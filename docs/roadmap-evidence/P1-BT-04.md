# P1-BT-04 — Build Track STEP 04（Update で当たり判定・再配置）を実装し、Hypot を Update 側で学習者が書く行として明示する

Status: PASS

## Required evidence

- [x] Edit target
- [x] Next lines
- [x] RULE challenge
- [x] Desktop
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Changes

- Files: `games/build-track/hit-reset/{main.go,main_test.go}` and
  `web/{ja,en}/build/hit-reset/index.html`.
- Behavior: the learner-visible `math.Hypot` collision is a pure `hit` helper
  called from Update; a hit changes score and deterministic target position,
  while Draw only reads the result.

## Commands and results

```text
go test ./games/build-track/hit-reset
PASS

GOOS=js GOARCH=wasm go build -trimpath -o /tmp/ebi-build-hit-reset.wasm ./games/build-track/hit-reset
PASS

bash scripts/build.sh --fast
PASS; dist/play/hit-reset/game.wasm built and 208/208 unchanged.

git diff --check
exit 0
```

## Manual checks

| Surface | Representative viewport | Input completed | Result / issue |
| --- | --- | --- | --- |
| Desktop | 1440 × 900 | Pointer | Circle is hit-tested with pointer coordinates; score and next target change in Update. |
| Tablet | 768 × 1024 | Touch | First just-pressed touch supplies the same collision coordinates. |
| Phone | 390 × 844 | Touch | Shared responsive WASM frame keeps the touch game available. |

- Japanese: highlights the exact Update-side Hypot line and named two-point rule.
- English: has the matching source path, Update-only rule, tests, and LEVEL 01 link.
- Readability / accessibility: localized iframe titles and explicit hit/miss prose
  do not make the mechanic depend on colour alone.
- Screenshots / recordings: the built WASM is the same artifact used by Pages.
