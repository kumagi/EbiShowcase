# P2-G04 — Command RPG (`rpg`).

Status: PASS

## Required evidence

- [x] Shader
- [x] Audio
- [x] Text/UI
- [x] Camera
- [x] Desktop
- [x] Tablet
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Integration

- Shader: a time-uniform Kage badge enriches the live quest HUD through its safe fallback helper.
- Audio: party attack and enemy damage synthesize distinct original one-shot feedback after input arms the gate.
- Text/UI: quest HP header uses shared `text/v2` Noto face.
- Camera: world grid position is synchronized into pure camera state, isolating future transition/shake composition from quest data.

## Verification

```text
go test ./internal/audiolab ./internal/cameralab ./internal/uilab ./internal/shaderlab
GOOS=js GOARCH=wasm go build ./games/tracks/rpg/ebi-quest
git diff --check
success
```

## Viewports and language

- Desktop/tablet/phone retain grid touch navigation and 1/2/3 battle actions in the responsive canvas.
- Shared Noto UI supports bilingual game text while existing quest/save state remains unchanged.
