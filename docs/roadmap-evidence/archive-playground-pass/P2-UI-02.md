# P2-UI-02 — Teach measurement, alignment, wrapping, and CJK line breaking.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Added pure `uilab.X` alignment and `uilab.Wrap`: English retains words when possible; Japanese selects rune-level safe break opportunities before pixel placement.
- Added a `text/v2` layout demo that toggles Japanese/English by keyboard, pointer, or touch and centers each line using `text.Advance` rather than guessed coordinates.
- Added alignment and bilingual-wrap tests.

## Commands and results

```text
gofmt -w internal/uilab/layout.go internal/uilab/layout_test.go games/tracks/ui-lab/text-layout/main.go
go test ./internal/uilab
GOOS=js GOARCH=wasm go build ./games/tracks/ui-lab/text-layout
git diff --check
success
```

## Manual review

- The centered text uses measured advance and stays inside the guide panel.
- Tap/Space makes the English word wrapping and Japanese CJK wrapping visibly comparable.
