# P0-07 — Record a clean baseline: tests, 208/208, VFX 29/29, lesson structure, and GitHub Pages build.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Files: `docs/roadmap-evidence/P0-01.md` through `P0-07.md`, plus the generated `dist/` verification artifact (ignored).
- Behavior: records the reproducible baseline that all later roadmap tasks must preserve or improve.

## Commands and results

```text
go test ./...
exit 0 (all Go packages and unit-test packages passed)

CI=1 bash scripts/build.sh --fast
exit 0; CI ignored --fast, injected OGP metadata, rendered all 557 OGP PNGs, built the Pages artifact, and passed site metadata checks.

node scripts/curriculum.mjs summary
{"total":208,"playable":208,"remaining":0,"vfx":{"total":29,"playable":29}}

node scripts/check-lessons.mjs
Checked 474 pages (237 playable lessons).
OK — all playable lessons meet the article skeleton.

node scripts/check-site-metadata.mjs
OK — 208/208 gated, 29/29 VFX, and 66 home cards are linked and bilingual.

test -f dist/index.html && test -f dist/play/flappy/game.wasm && test -f dist/play/ebi-rhythm/game.wasm && test -f dist/play/ebi-defense/game.wasm && test -f dist/play/ebi-adventure/game.wasm
Pages artifact targets present

git diff --check
exit 0
```

## Manual checks

| Surface | Representative viewport | Input completed | Result / issue |
| --- | --- | --- | --- |
| Desktop | 1440 × 900 | Keyboard + pointer | Not applicable: baseline is a structural/build audit; individual game audits begin in P1. |
| Tablet | 768 × 1024 | Touch | Not applicable: baseline is a structural/build audit; individual game audits begin in P1. |
| Phone | 390 × 844 | Touch | Not applicable: baseline is a structural/build audit; individual game audits begin in P1. |

- Japanese: all 237 playable JA pages passed the lesson and metadata checks.
- English: all 237 playable EN pages passed the lesson and metadata checks.
- Readability / accessibility: not a visual redesign task; the next phase requires explicit three-surface gameplay audits.
- Screenshots / recordings: generated Pages artifact includes representative final-game WASM files; runtime screenshots are task-specific evidence in P1 onward.
