# P2-SH-05 — Teach offscreen passes, downsampling, and controlled faux/real blur.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Added `offscreen-blur`, a playable pass diagram: render the world to a 120×120 image, then choose crisp upscale, enlarged downsample presentation, or controlled faux bloom from four translucent copies.
- Added the fifth bilingual Shader Lab panel, teaching the explicit `world → offscreen → final pass` pipeline and when a cheap approach is preferable to full blur.

## Commands and results

```text
gofmt -w games/tracks/shader-lab/offscreen-blur/main.go
GOOS=js GOARCH=wasm go build ./games/tracks/shader-lab/offscreen-blur
node scripts/gen-shader-lab.mjs
git diff --check
success
```

## Manual review

- Tap, mouse, and Space cycle all three presentation paths.
- The same offscreen image is reused for every pass, making allocation and composition boundaries visible.
- Japanese and English text explains downsampling and deliberate faux blur without claiming it is a required gameplay effect.
