# P2-G07 — Deckbuilder (`deckbuilder`).

Status: PASS

## Required evidence

- [x] Shader
- [x] Audio
- [x] Text/UI
- [x] Camera
- [x] Desktop
- [x] Tablet
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Integration and verification

- `ebi-ascent` keeps its complete five-floor card run, enemy intents, card rewards, routes, animated hit effects, mouse/touch controls, and replay score. Card play and ending a turn now use the shared original waveform SE; the run header uses the shared Noto text helper; and the shader-lab pulse badge makes the shared rendering path visible without obscuring the game.
- The game retains its 480×720 portrait layout and pointer/touch hit areas, so the same interaction model fits desktop, tablet, and phone embeds. Shared Noto UI faces support both Japanese and English lesson shells.

```text
go test ./internal/audiolab ./internal/cameralab ./internal/uilab ./internal/shaderlab
GOOS=js GOARCH=wasm go build ./games/tracks/deckbuilder/ebi-ascent
git diff --check
success
```
