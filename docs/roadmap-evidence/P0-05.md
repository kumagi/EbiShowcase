# P0-05 — 薄い main.go ラッパーと snippet 不一致ページの棚卸し表を証跡に残す

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Inventory

| Track | Entry-file evidence | Page mismatch / missing authoring map | Phase 2 repair |
| --- | --- | --- | --- |
| Rhythm | 7 `main.go` files, 8–18 lines. `beat-pulse` builds a `MakeChart` then calls `rhythmplay.Run(Config)`. | `q.code` displays a core fragment, but the page does not show the editable entry or name `MakeChart`/`Taps` as the edit location. The challenge says to add notes without a path. | `P2-RHY-GEN`: entry + `internal/rhythmplay` excerpt + chart-data RULE. |
| Raycaster | 6 `main.go` files, each a six-line `raycasterui.Run(Variant)` wrapper. | `q.code` shows ray/projection logic rather than the variant entry. FOV/ray-count CHANGE and door/patrol challenge have no concrete edit path. | `P2-RAY-GEN`: variant entry + `internal/raycasterui` excerpt + map/mission RULE. |
| Tower Defense | 8 `main.go` files, each a five-line `towerdefenseplay.Run(Config{Step, Title})` wrapper. | The generator calls its panel `GO + EBITENGINE` and displays a concept snippet. Its primary challenge is “Change one value” (range/speed/cost), not a path-labelled RULE. | `P2-TD-GEN`: config/data-table entry + play excerpt + wave/enemy-row RULE. |
| Reversi | 5 `main.go` files, each a six-line `reversiui.Run(Variant)` wrapper. | The panel describes a central rule but does not show the variant entry. The challenge asks to change `ScoreMap`/look-ahead with neither path nor named non-tuning rule. | `P2-REV-GEN`: variant/CPU entry + evaluator excerpt + named positional-evaluation RULE. |
| Top-down Adventure | 8 `main.go` files, each a five-line `topdownadventuregame.Run(lesson)` wrapper. | The generator displays `q.code` while the actual entry is only a lesson number. The challenge asks for “one value or data record” without a path or function/table. | `P2-TOP-GEN`: lesson/room entry + game-data excerpt + key/door/room RULE. |

## Changes

- Files: `docs/roadmap-evidence/P0-05.md`.
- Behavior: this evidence turns the five shared-engine tracks into an explicit,
  ordered Phase 2 queue. It does not pretend the thin wrappers themselves are
  defective; the defect is that pages hide the real edit surface or ask for an
  untraceable tuning task.

## Commands and results

```text
find games/tracks/{rhythm,raycaster,tower-defense,reversi,topdown-adventure} -name main.go
Rhythm: 7 entries; Raycaster: 6; Tower Defense: 8; Reversi: 5; Top-down Adventure: 8.

wc -l representative main.go files
Rhythm 8 lines; Raycaster 6; Tower Defense 5; Reversi 6; Top-down Adventure 5.

rg -n 'code-lesson|CHALLENGE|Change one value|数値かデータを1つ変えて' scripts/gen-{rhythm,raycaster,tower-defense,reversi,topdown-adventure}-track.mjs
exit 0: confirms the generated panels and challenge wording audited above.

git diff --check
exit 0
```

## Manual checks

| Surface | Representative viewport | Input completed | Result / issue |
| --- | --- | --- | --- |
| Desktop | 1440 × 900 | Keyboard + pointer | Reviewed generated page templates against the representative entry source. |
| Tablet | 768 × 1024 | Touch | Not applicable to this inventory-only task. |
| Phone | 390 × 844 | Touch | Not applicable to this inventory-only task. |

- Japanese: the mismatch descriptions identify the missing Japanese edit map and RULE instruction.
- English: the same gaps are present in the shared bilingual generators.
- Readability / accessibility: each row distinguishes a legitimate thin wrapper
  from the learner-facing documentation gap it creates.
- Screenshots / recordings: not applicable; this task records the pre-fix state.
