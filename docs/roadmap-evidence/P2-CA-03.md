# P2-CA-03 — Teach dead zones, look-ahead, and target switching.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Added `FollowDeadZone`: target movement within a half-width/height does not move the camera; velocity times look-ahead shifts the desired target outside the zone.
- Added an interactive dead-zone/l​ook-ahead demo with visible safe rectangle and direction switching by keyboard, pointer, and touch.
- Added pure test coverage for no movement inside the zone and velocity-triggered look-ahead.

## Commands and results

```text
gofmt -w internal/cameralab/*.go games/tracks/camera-lab/deadzone-lookahead/main.go
go test ./internal/cameralab
GOOS=js GOARCH=wasm go build ./games/tracks/camera-lab/deadzone-lookahead
git diff --check
success
```

## Manual review

- The safe rectangle and ahead-facing target behaviour explain why a camera need not center every frame.
