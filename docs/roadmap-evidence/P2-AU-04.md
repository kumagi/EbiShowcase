# P2-AU-04 — Teach reusable SE voices/queues without per-hit allocation spikes.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Added fixed-capacity `audiolab.Queue`; it overwrites the oldest request at capacity instead of allocating/growing in a hit storm.
- Added a visual voice-queue WASM demo. Each tap adds three HIT requests, one is consumed per update, and four fixed slots show backlog behaviour.
- Added a ring-buffer test that proves overflow retains the two newest requests.

## Commands and results

```text
gofmt -w internal/audiolab/queue.go internal/audiolab/queue_test.go games/tracks/audio-lab/voice-queue/main.go
go test ./internal/audiolab
GOOS=js GOARCH=wasm go build ./games/tracks/audio-lab/voice-queue
git diff --check
success
```

## Manual review

- Keyboard, pointer, and touch enqueue repeated hits.
- The four slot display makes bounded buffering visible, including rapid repeated input.
