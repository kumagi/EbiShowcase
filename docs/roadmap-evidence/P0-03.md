# P0-03 — サイト定型文「値を変えて / change values」を「1ルール足して確かめる / add one rule and verify」系へ置換する（生成器・OGP・ガイド・卒業ページのソースを含む）

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Files: `scripts/inject-ogp.mjs`, `scripts/enrich-thin-labs.mjs`, regenerated
  OGP metadata in `web/` and `web/assets/og/manifest.json`.
- Behavior: the shared OGP description now asks learners to add and verify one
  Go rule. The injector removes both complete and truncated legacy tuning-copy
  before adding the new text, so future metadata rebuilds cannot restore it.
  The thin energy lab similarly asks for a refill rule before optional tuning.

## Commands and results

```text
node scripts/inject-ogp.mjs
OGP injected into 257/579 HTML files; manifest updated for 579 pages.

node scripts/enrich-thin-labs.mjs
Updated the bilingual thin-lab pages, including the turn-energy RULE prompt.

rg -n --glob '!dist/**' --glob '!node_modules/**' --glob '!docs/**' --glob '!scripts/inject-ogp.mjs' \
  '値を変えて結果を確かめ|Play the demo, read a short Go example, change a value, and apply' web scripts graduation examples
exit 1: no stale shared tuning-only template remains.

git diff --check
exit 0
```

## Manual checks

| Surface | Representative viewport | Input completed | Result / issue |
| --- | --- | --- | --- |
| Desktop | 1440 × 900 | Keyboard + pointer | Metadata copy only; no interactive control changed. |
| Tablet | 768 × 1024 | Touch | Metadata copy only; no interactive control changed. |
| Phone | 390 × 844 | Touch | Metadata copy only; no interactive control changed. |

- Japanese: shared text now says 「Goで1つルールを足して確かめ」.
- English: shared text now says “add one Go rule, verify it”.
- Readability / accessibility: the short description now promises an observable
  authoring action instead of only a number-tuning action.
- Screenshots / recordings: not applicable; OGP bitmap regeneration is deferred
  to the required full verification at the phase boundary.
