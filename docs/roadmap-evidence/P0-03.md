# P0-03 — Add an automated metadata check for counts, bilingual pairs, thumbnails, and final-game links.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Files: `scripts/check-site-metadata.mjs`, `scripts/home-thumbnails.mjs`, `scripts/build.sh`, and `scripts/ralph-loop.sh`.
- Behavior: CI/local builds now reject a changed curriculum gate count, missing JA/EN lesson page, stale/missing home-card WebP thumbnail, or genre home card whose completion page/source/iframe target is invalid.

## Commands and results

```text
node --check scripts/check-site-metadata.mjs
node --check scripts/home-thumbnails.mjs
exit 0

node scripts/check-site-metadata.mjs
OK — 208/208 gated, 29/29 VFX, and 66 home cards are linked and bilingual.

node scripts/check-site-metadata.mjs --json
{"failures":0,"ok":true}

git diff --check
exit 0
```

## Manual checks

- Viewport / input: not applicable; this is a release metadata gate and leaves game interaction unchanged.
- Japanese / English: the checker opens each curriculum page in both `web/ja` and `web/en`, and verifies each home page embeds the same mapped thumbnail.
- Screenshots: every mapped thumbnail is required to be an existing RIFF/WEBP file; the generated manifest is checked for staleness.
- Final-game links: every one of the 25 genre cards is traced to a real `games/tracks/<track>/<final>/main.go` implementation and a JA/EN completion article iframe using that final game's play slug.
