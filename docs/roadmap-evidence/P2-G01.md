# P2-G01 — Platform action (`platformer`).

Status: PASS

## Required evidence

- [x] Shader
- [x] Audio
- [x] Text/UI
- [x] Camera
- [x] Desktop
- [x] Tablet
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Integration

- Shader: the completion game owns `shaderlab.Pulse` and renders a time-uniform pulsing status badge with its normal draw fallback retained by the shared helper.
- Audio: jump and coin events synthesize original one-shot SE after the first input arms the audio gate.
- Text/UI: the production HUD uses the shared `text/v2` bilingual-capable Noto face rather than final-screen DebugPrint.
- Camera: existing stage follow camera is mirrored into pure `cameralab.State`, retaining world and display coordinates separately.
- Stages/replay: existing four stages, power-up, enemies, score, and touch restart remain intact.

## Verification

```text
go test ./internal/audiolab ./internal/cameralab ./internal/uilab ./internal/shaderlab
GOOS=js GOARCH=wasm go build ./games/tracks/platformer/powerup-adventure
git diff --check
success
```

## Viewports and language

- Desktop/tablet/phone: the 480×720 responsive shell preserves keyboard plus lower-half movement/upper-half jump touch affordances.
- English labels are current platformer HUD copy; Japanese-capable face support is present through `uilab.Face` for translated platformer content as it is introduced.
