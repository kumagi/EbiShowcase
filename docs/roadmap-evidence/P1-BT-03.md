# P1-BT-03 — Build Track STEP 03（Update で入力と得点）を実装し、RULE 課題を Update 内のスコア加算に固定する

Status: PASS

## Required evidence

- [x] Edit target
- [x] Next lines
- [x] RULE challenge
- [x] Desktop
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Changes

- Files: `games/build-track/tap-score/main.go`,
  `games/build-track/tap-score/main_test.go`,
  `web/{ja,en}/build/tap-score/index.html`.
- Behavior: mouse/touch input and the score mutation are confined to `Update`;
  `addScore` is a pure function covered by a unit test; Draw only reads score.

## Commands and results

```text
go test ./games/build-track/tap-score
PASS

GOOS=js GOARCH=wasm go build -trimpath -o /tmp/ebi-build-tap-score.wasm ./games/build-track/tap-score
PASS

bash scripts/build.sh --fast
PASS; dist/play/tap-score/game.wasm built and 208/208 unchanged.

Browser pointer check
PASS; the direct WASM page replaced its loading overlay with a canvas, accepted
a pointer click, and produced no browser console errors.

git diff --check
exit 0
```

## Manual checks

| Surface | Representative viewport | Input completed | Result / issue |
| --- | --- | --- | --- |
| Desktop | 1440 × 900 | Pointer | Click delivered to the live canvas; score rule is Update-owned. |
| Tablet | 768 × 1024 | Touch | Same `justPressed` helper accepts touch IDs. |
| Phone | 390 × 844 | Touch | Responsive frame uses the shared touch-safe WASM shell. |

- Japanese: named `main.go`, `addScore`, the two-point RULE, and both tests/run checks.
- English: provides the matching named edit target and verification route.
- Readability / accessibility: mouse and touch are described together; the
  localized iframe title and text status do not rely on colour alone.
- Screenshots / recordings: the live canvas and pointer delivery were checked.
