# P2-G03 — Idle/clicker (`clicker`).

Status: PASS

## Required evidence

- [x] Shader
- [x] Audio
- [x] Text/UI
- [x] Camera
- [x] Desktop
- [x] Tablet
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Integration

- Shader: a time/value-driven `shaderlab.Pulse` badge gives bakery production a visible live effect with safe fallback.
- Audio: baking and oven purchase use distinct original one-shot SE after input arms the audio gate.
- Text/UI: sweets HUD is rendered through shared `text/v2` Noto face.
- Camera: factory composition keeps a pure `cameralab.State` at its stable static-board framing point.

## Verification

```text
go test ./internal/audiolab ./internal/cameralab ./internal/uilab ./internal/shaderlab
GOOS=js GOARCH=wasm go build ./games/tracks/clicker/idle-factory
git diff --check
success
```

## Viewports and language

- Desktop/tablet/phone retain Space/tap baking and B/shop touch action on the responsive canvas.
- Shared Noto UI face supports Japanese and English game strings; control labels remain clear with icon-free text affordances.
