# P1-BT-01 — Build Track STEP 01（空のゲームループ）の Go + 日英ページを実装し、「次に足す行」枠と公理の指差しを付ける

Status: PASS

## Required evidence

- [x] Edit target
- [x] Next lines
- [x] RULE challenge
- [x] Desktop
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Changes

- Files: `games/build-track/empty-loop/main.go`, `web/ja/build/empty-loop/index.html`,
  `web/en/build/empty-loop/index.html`, `scripts/build.sh`, `web/style.css`.
- Behavior: adds an ungated, source-downloadable Ebitengine empty-loop demo.
  Both pages expose the whole running entry file, a narrowly scoped next-lines
  panel, the Update/Draw axiom, and the path from setup to LEVEL 01.

## Commands and results

```text
gofmt -w games/build-track/empty-loop/main.go
exit 0

go test ./games/build-track/empty-loop
PASS (no test files)

bash scripts/build.sh --fast
PASS; built dist/play/empty-loop/game.wasm and preserved the 208/208 gate.

Desktop browser check, 1440 × 900
PASS; one visible lesson iframe (405 × 608), one canvas in the iframe, loading
overlay removed, and no console errors.

Phone-width responsive check
PASS; English page has one iframe, one NEXT LINES panel, and no console errors.

git diff --check
exit 0
```

## Manual checks

| Surface | Representative viewport | Input completed | Result / issue |
| --- | --- | --- | --- |
| Desktop | 1440 × 900 | Pointer | Empty loop intentionally has no game input; WASM canvas loaded without errors. |
| Tablet | 768 × 1024 | Touch | Empty loop intentionally has no game input; normal responsive CSS applies. |
| Phone | 390 × 844 | Touch | English page retained one visible frame and NEXT LINES panel without console errors. |

- Japanese: setup is written as browser download → local Go workspace → run,
  and the public source map is named.
- English: has the same clone-free setup, axiom, next-line, and pager content.
- Readability / accessibility: iframe titles are localized; the source link and
  Copy button have accessible text; no instruction depends on a screen colour.
- Screenshots / recordings: the browser check confirmed the live canvas rather
  than a static image; no screenshot was needed for the intentionally empty demo.
