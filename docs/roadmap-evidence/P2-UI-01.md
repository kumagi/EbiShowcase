# P2-UI-01 — Register selected font licenses/notices and create a `text/v2` bilingual font loader.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Added `internal/uilab.Face`, a cached Ebitengine `text/v2` GoTextFaceSource over the existing Noto Sans JP OFL asset. It returns language-tagged Japanese/English faces from one glyph-complete font source.
- Updated the Noto notice to include `text/v2` UI use and recorded the `text/v2` transitive shaping dependencies in the third-party inventory.
- Added required Go module checksums for `github.com/go-text/typesetting` and `github.com/rivo/uniseg`.

## Commands and results

```text
go get github.com/hajimehoshi/ebiten/v2/text/v2@v2.9.9
gofmt -w internal/uilab/*.go
go test ./internal/uilab
ok github.com/kumagi/EbiShowcase/internal/uilab
git diff --check
success
```

## Manual review

- `Face("ja", size)` and `Face("en", size)` share a cached source and set their matching BCP-47 language hint.
- Noto Sans JP covers the Japanese and Latin UI strings without a glyph fallback; its OFL source, full text, and usage are documented.
