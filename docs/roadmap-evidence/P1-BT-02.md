# P1-BT-02 — Build Track STEP 02（game の値を Draw が投影）を実装し、Draw で状態を進める反例を1つ示してから正しい差分へ導く

Status: PASS

## Required evidence

- [x] Edit target
- [x] Next lines
- [x] RULE challenge
- [x] Desktop
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Changes

- Files: `games/build-track/state-picture/main.go`,
  `games/build-track/state-picture/main_test.go`,
  `web/{ja,en}/build/state-picture/index.html`.
- Behavior: `game` owns the initial square coordinates; `Draw` reads and
  projects them only. The article first shows the prohibited `g.x++` in Draw,
  then the actual projection code and the next Update-side score rule.

## Commands and results

```text
gofmt -w games/build-track/state-picture/main.go games/build-track/state-picture/main_test.go
exit 0

go test ./games/build-track/state-picture
PASS

GOOS=js GOARCH=wasm go build -trimpath -o /tmp/ebi-build-state-picture.wasm ./games/build-track/state-picture
PASS

bash scripts/build.sh --fast
PASS; dist/play/state-picture/game.wasm is built and the 208/208 gate remains intact.

Desktop browser check
PASS; Japanese page exposes exactly one game iframe; its canvas appeared after
WASM load, the loading overlay disappeared, and the counterexample plus correct
Draw projection are visible in the article.

Phone responsive check
PASS; the English page retains the iframe and NEXT LINES section under the
shared mobile stylesheet, with no lesson-script errors.

git diff --check
exit 0
```

## Manual checks

| Surface | Representative viewport | Input completed | Result / issue |
| --- | --- | --- | --- |
| Desktop | 1440 × 900 | Pointer | Static STEP: canvas loaded; position is intentionally unchanged until the learner edits it. |
| Tablet | 768 × 1024 | Touch | Static STEP: no game input is introduced yet; shared responsive shell remains applicable. |
| Phone | 390 × 844 | Touch | English page kept one demo frame and the next-lines path without layout/script regression. |

- Japanese: explains the state → Draw → pixels mapping and the Draw-side
  counterexample before the correct version.
- English: carries the same mapping, counterexample, named edit target, and
  verification commands.
- Readability / accessibility: the iframe has a localized title; the
  counterexample uses explicit ❌/✅ labels rather than colour alone.
- Screenshots / recordings: live WASM canvas was checked instead of relying on
  a static thumbnail.
