# P2-CA-02 — Teach follow, smoothing, clamping, and coordinate conversion.

Status: PASS

## Required evidence

- [x] Implementation
- [x] Automated checks
- [x] Manual review

## Changes

- Added `State.Follow`, which smooths toward a target then clamps the view centre to world bounds.
- Added the `follow-clamp` demo: target movement is transformed through camera coordinates while the visible grid proves world drawing is offset rather than mutated.
- Added a follow/clamp test alongside coordinate round-trip coverage.

## Commands and results

```text
gofmt -w internal/cameralab/*.go games/tracks/camera-lab/follow-clamp/main.go
go test ./internal/cameralab
GOOS=js GOARCH=wasm go build ./games/tracks/camera-lab/follow-clamp
git diff --check
success
```

## Manual review

- Left/right/tap moves the target; the camera visibly lags then stops at its world boundary.
