# P2-G08 — Match-three (`match3`).

Status: PASS

## Required evidence

- [x] Shader
- [x] Audio
- [x] Text/UI
- [x] Camera
- [x] Desktop
- [x] Tablet
- [x] Phone
- [x] Japanese
- [x] English
- [x] Tests

## Integration and verification

- `ebi-match` preserves its three playable reef stages, valid-swap safety net, animated swaps/falls, cascades, special pieces, score goals, keyboard, pointer, and touch controls. Successful swaps and each resolving chain now produce short original synthesized feedback; its title uses the shared Noto UI helper and the shader pulse badge confirms the shared VFX path.
- The fixed portrait board and pointer-sized cells remain suitable for desktop, tablet, and phone embeds. Shared Noto faces provide Japanese and English shell compatibility.

```text
go test ./internal/audiolab ./internal/cameralab ./internal/uilab ./internal/shaderlab
GOOS=js GOARCH=wasm go build ./games/tracks/match3/ebi-match
git diff --check
success
```
