# Feedback operations

At 20 responses, run `node scripts/feedback-thresholds.mjs 20`, review the newest 20 rows, and mark each item handled or not planned. At 500 responses, export/archive the rows, then clear the live response sheet so new form responses begin at row 2. Keep the archive read-only.
