# 60-second arcade

Move with arrows/WASD, collect stars for 60 seconds, and avoid the red zone.
First copy `starter/`, then compare with `reference/` only after your version
has a score, timer, restart, and a rule test. Apache-2.0.
